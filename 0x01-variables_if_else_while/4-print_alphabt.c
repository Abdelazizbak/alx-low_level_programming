#include <stdio.h>
#include <ctype.h>
/**
*main - Program to print alphabet letters except q and e followed y new line
*
*Return: return 0
*/
int letter;
for (letter = 'a'; letter <= 'z' ; letter++)
{
continue;
}
letter = tolower(letter);
putchar(letter);
}
putchar('\n');
return (0);
}
